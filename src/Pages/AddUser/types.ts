export type FormData = {
    firstName?: string;
    surName?: string;
    number: string;
    email: string;
    photoUrl?: string;
  }
export type TFieldValues = {
  firstName: string;
  surName: string;
  number: string;
  email: string;
  photoUrl: string;
};


