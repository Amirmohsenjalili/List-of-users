import { SxProps } from '@mui/material';

const myBox: SxProps = {
    mt: `100px`,
}

const myAlert: SxProps = {
    width: '100%'
}

const myInput: SxProps = {
    width: '100%'
}

export default { myBox, myAlert, myInput };