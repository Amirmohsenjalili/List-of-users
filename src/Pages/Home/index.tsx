import Layout from '../../Layout';
import User from '../../Components/UsersCard'

const index = () => {
    return (
        <Layout>
            <User />
        </Layout>
    );
};

export default index;
